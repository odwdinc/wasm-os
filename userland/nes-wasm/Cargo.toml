[workspace]

[package]
name = "nes-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]
name = "nes"

[dependencies]
runes = { git = "https://github.com/Determinant/runes" }
wee_alloc = { version = "0.4", default-features = false }

[profile.release]
opt-level = 3
lto = true
